# RecallHub V1.0 项目设计文档

版本：1.0
项目名：RecallHub
仓库名：`recall-hub`
定位：个人记忆、日记、待办与提醒系统

***

# 1. 项目定位

RecallHub 是一个长期运行在个人电脑上的私人信息系统。

它负责保存和展示：

* 日记
* 待办
* 提醒
* 想法
* 备忘
* OpenClaw 提取出的有效聊天内容
* 可选的原始聊天记录
* 历史时间线
* 搜索结果

OpenClaw 负责交流。

RecallHub 负责记住。

最终使用方式：

```
微信 / Telegram / QQ / 语音
            │
            ▼
        OpenClaw
            │
      RecallHub Skill
            │
            ▼
      RecallHub REST API
            │
            ▼
          MySQL

```

查看数据时：

```
手机 / PC 浏览器
        │
        ▼
   RecallHub Web
        │
        ▼
 RecallHub REST API
        │
        ▼
      MySQL

```

提醒时：

```
MySQL Reminder
       │
       ▼
RecallHub Scheduler
       │
       ▼
OpenClaw /hooks/agent
       │
       ▼
微信 / TG / QQ
       │
       ▼
      用户

```

目前已经实际验证：

```
HTTP Hook
    ↓
OpenClaw
    ↓
微信

```

可以正常发送消息。

***

# 2. 核心目标

RecallHub V1 解决五件事。

## 2.1 快速记录

用户想到一件事情时，不需要打开 RecallHub。

直接告诉 OpenClaw：

> 帮我记一下，RecallHub 以后可以增加月度回顾。

OpenClaw 写入：

```
IDEA
RecallHub 以后可以增加月度回顾

```

***

## 2.2 待办

例如：

> 周末研究一下 Jev。

生成：

```
TODO
研究 Jev

```

***

## 2.3 提醒

例如：

> 明天下午三点提醒我交电费。

生成：

```
TODO
交电费

Reminder
2026-09-21 15:00

```

时间到了以后主动给用户发微信。

***

## 2.4 日记

用户可以：

* 网页自己写
* 和 OpenClaw 聊天时产生
* 从语音聊天中整理

例如：

> 今天下午终于把登录接口的问题解决了。

保存：

```
DIARY
今天下午解决了登录接口的问题。

```

一天可以存在多个日记片段。

***

## 2.5 查看与搜索

RecallHub Web 负责解决：

> 我最近都在做什么？

> 今天还有什么没完成？

> 上个月有什么想法？

> 我以前什么时候提到过 Jev？

> 最近的日记是什么？

***

# 3. 系统边界

## RecallHub 负责

```
长期保存
数据展示
搜索
提醒状态
提醒调度
待办状态
日记
主题
备份
导出
API

```

## OpenClaw 负责

```
聊天
语音
自然语言理解
信息提取
调用 RecallHub
把提醒发送到微信 / TG / QQ

```

## MySQL 负责

保存所有长期状态。

RecallHub 数据库是唯一可信数据源。

OpenClaw 中的聊天记录、Cron 或 Session 都不能替代 RecallHub 数据库。

***

# 4. V1 不做的内容

第一版暂时不实现：

* 多用户
* 团队协作
* Android 原生 App
* iOS 原生 App
* 知识图谱
* 复杂项目管理
* 甘特图
* 类 Notion 编辑器
* 大型文件系统
* 自动生成大量统计
* 向量数据库
* Jev 强依赖
* 自动标签系统

这些功能以后根据真实使用情况增加。

***

# 5. 技术基线

## 后端

```
Java 17
Spring Boot 3.5.x
Maven
Spring Web
Spring Security
Spring Scheduling
Spring Boot Actuator
MyBatis
MyBatis-Plus
Flyway
Jackson
Jakarta Validation

```

Spring Boot 3.5 当前最低 Java 要求为 Java 17，因此和现有 JDK 17 环境可以直接配合。

***

# 6. 数据库

使用现有数据库：

```
MySQL 5.7.23

```

数据库统一：

```
ENGINE = InnoDB
CHARSET = utf8mb4
COLLATE = utf8mb4_unicode_ci

```

不使用 MySQL 8 专属功能。

项目 SQL 必须兼容：

```
MySQL 5.7.23

```

包括 Flyway migration。

***

# 7. MySQL 5.7 约束

允许使用：

```
JSON
FULLTEXT
ngram
事务
行锁
InnoDB

```

MySQL 5.7 已提供原生 JSON 数据类型，并会对保存的 JSON 文档进行校验。

MySQL 5.7 也提供用于中文、日文和韩文的 ngram 全文解析器，可以用于 InnoDB FULLTEXT 搜索。

不使用：

```
CTE
窗口函数
SKIP LOCKED
NOWAIT
MySQL 8 Collation

```

因此提醒调度不能依赖：

```
FOR UPDATE SKIP LOCKED

```

V1 使用条件 UPDATE 抢占任务。

***

# 8. 前端技术栈

使用：

```
Vue 3
TypeScript
Vite
Vue Router
Pinia
Tailwind CSS
Axios

```

基础交互组件可以自己封装。

不再让 Element Plus 主导视觉体系。

原因是 RecallHub 从第一版开始需要支持高度自由的 CSS Variable 主题。

前端组件必须尽可能只依赖：

```
var(--background)
var(--foreground)
var(--card)
var(--primary)
...

```

而不是在组件内部写死颜色。

***

# 9. UI 风格

RecallHub 默认 UI 使用此前确定的设计方向：

* 浅色
* 大量留白
* 蓝色作为默认强调色
* 柔和卡片
* 手机和桌面均可使用
* 不做企业管理后台风格
* 不做 Notion 克隆

桌面主要结构：

```
Sidebar
    │
    ├── 今天
    ├── 时间线
    ├── 日记
    ├── 搜索
    └── 设置

Main Content

```

移动端使用底部导航。

***

# 10. 页面结构

V1 一级页面共五个：

```
今天
时间线
日记
搜索
设置

```

辅助界面：

```
记录详情
记录编辑
新增记录
提醒编辑
主题管理
主题编辑器
登录

```

***

# 11. 「今天」页面

这是 RecallHub 使用频率最高的页面。

主要展示：

```
今天日期

快速记录

已超时

今天提醒

待办

今天记录

```

结构：

```
9 月 20 日 星期日

┌────────────────────────────┐
│ 记点什么……                 │
└────────────────────────────┘

已超时
────────────────────────────
□ 续服务器

今天提醒
────────────────────────────
15:00  取快递
20:00  回复消息

待办
────────────────────────────
□ 研究 Jev
□ 整理 RecallHub

今天记录
────────────────────────────
想法  RecallHub 增加月度回顾
日记  今天上午……

```

***

# 12. 「时间线」页面

用于查看最近发生的事情。

数据按日期分组：

```
9 月 20 日

20:15
想法
增加月度回顾

16:40
已完成
修复登录接口

11:20
日记
今天上午整理了 RecallHub 的结构

09:05
待办
研究 Jev

```

过滤条件：

```
全部
日记
待办
想法
备忘

```

***

# 13. 「日记」页面

桌面端：

```
左侧

月历
+
本月日记列表


右侧

当天整理后的日记

原始片段

```

例如：

```
2026 年 9 月 20 日

整理后的日记
──────────────────

今天……


原始片段
──────────────────

09:20
阳光很好……

14:30
整理了一下工作……

22:00
晚上……

```

***

# 14. 「搜索」页面

顶部提供一个大搜索框：

```
搜索你的记录……

```

支持过滤：

```
全部
日记
待办
想法
备忘

```

结果显示：

```
类型
日期
标题
内容摘要
关键词高亮

```

***

# 15. 「设置」页面

设置包括：

```
账户与安全

外观与主题

OpenClaw

提醒渠道

聊天记录保存

备份与导出

```

***

# 16. 核心内容模型

RecallHub 中长期内容统一称为：

```
Entry

```

类型：

```
TODO
DIARY
NOTE
IDEA

```

状态：

```
ACTIVE
DONE
ARCHIVED

```

Reminder 不是 Entry 类型。

它是附加在 Entry 上面的提醒行为。

***

# 17. entries 表

```
entries
────────────────────────

id                  BIGINT
type                VARCHAR(20)

title               VARCHAR(255)
content             LONGTEXT

status              VARCHAR(20)

occurred_at         DATETIME(3)
due_at              DATETIME(3)
completed_at        DATETIME(3)

source_id           BIGINT

created_at          DATETIME(3)
updated_at          DATETIME(3)
deleted_at          DATETIME(3)

```

***

# 18. Entry 时间含义

`created_at`

用户什么时候记录。

`occurred_at`

事情实际什么时候发生。

例如：

```
下午 3 点出去办事
晚上 10 点才写日记

```

则：

```
occurred_at = 15:00
created_at  = 22:00

```

`due_at`

待办截止时间。

它不等于提醒时间。

***

# 19. sources 表

记录 Entry 来自哪里。

```
sources
────────────────────────

id                  BIGINT

source_type         VARCHAR(20)
channel             VARCHAR(20)

conversation_id     VARCHAR(255)
message_id          VARCHAR(255)

raw_content         LONGTEXT

metadata            JSON

created_at          DATETIME(3)

```

***

# 20. source\_type

支持：

```
WEB
OPENCLAW
IMPORT
SYSTEM

```

channel：

```
WEB
WECHAT
TELEGRAM
QQ
VOICE
UNKNOWN

```

例如：

```
source_type = OPENCLAW
channel     = WECHAT

```

***

# 21. 原始聊天保存

提供三种模式。

```
NONE
ON_DEMAND
ALL

```

默认：

```
ON_DEMAND

```

***

## NONE

只保存提取后的 Entry。

原始聊天不保存。

***

## ON\_DEMAND

默认只保存整理内容。

用户明确要求：

> 把刚才这段聊天也保存下来。

才保存 transcript。

***

## ALL

尽可能保存：

```
原始聊天
+
整理内容

```

***

# 22. OpenClaw Skill 的能力边界

Skill 本质上是一个带 `SKILL.md` 的指令目录，用来告诉 Agent 什么时候以及怎样使用已有工具。OpenClaw 当前会从 workspace `skills/` 等目录加载这类 Skill。

因此 V1：

```
OpenClaw
↓
RecallHub Skill
↓
Helper Script
↓
RecallHub REST API

```

可以完成：

```
主动记录
搜索
创建待办
创建提醒
完成待办
延后提醒

```

但单靠 Skill 不保证能够自动监听每次语音会话结束。

以后如果需要：

```
每次语音结束
↓
自动得到 transcript
↓
自动保存

```

再开发 OpenClaw Plugin。

OpenClaw 官方也把 Skill 和 Plugin 分开：Skill 适合工作流程指令，Plugin 则适合新增程序能力、生命周期 Hook、渠道等。

***

# 23. reminders 表

```
reminders
────────────────────────

id                      BIGINT

entry_id                BIGINT
notification_target_id  BIGINT

remind_at               DATETIME(3)

status                  VARCHAR(20)

attempt_count           INT
next_attempt_at         DATETIME(3)

lease_until             DATETIME(3)

last_error              VARCHAR(1000)

created_at              DATETIME(3)
updated_at              DATETIME(3)
sent_at                 DATETIME(3)
cancelled_at            DATETIME(3)

```

状态：

```
SCHEDULED
PROCESSING
SENT
FAILED
CANCELLED

```

***

# 24. Reminder 为什么归 RecallHub 管

例如：

```
9 月 21 日 15:00
交电费

```

真正保存的是：

```
entries

TODO
交电费

```

以及：

```
reminders

2026-09-21 15:00

```

OpenClaw 不保存调度时间。

网页里如果把时间从：

```
15:00

```

改成：

```
16:00

```

只修改 MySQL。

不存在 OpenClaw Cron 和数据库不同步的问题。

***

# 25. Reminder Worker

使用 Spring Scheduling。

例如：

```
每 10 秒扫描一次

```

查询：

```
status = 'SCHEDULED'
AND remind_at <= NOW()
AND (
    next_attempt_at IS NULL
    OR next_attempt_at <= NOW()
)

```

***

# 26. MySQL 5.7 Reminder 抢占

由于不能依赖 MySQL 8 的 `SKIP LOCKED`，V1 使用条件更新。

先找到候选 ID。

对每条执行：

```
UPDATE reminders
SET
    status = 'PROCESSING',
    lease_until = ?
WHERE
    id = ?
    AND status = 'SCHEDULED'
    AND remind_at <= NOW();

```

只有：

```
affectedRows = 1

```

当前 Worker 才拥有这条任务。

***

# 27. Reminder Lease

进入：

```
PROCESSING

```

以后设置：

```
lease_until = NOW + 2 minutes

```

如果 RecallHub 在投递过程中崩溃：

```
PROCESSING
AND lease_until < NOW()

```

可以重新恢复到待处理状态。

***

# 28. 关机后的提醒

假设：

```
15:00 提醒

```

但电脑：

```
14:50 关机
16:10 开机

```

RecallHub 启动后仍会查询：

```
remind_at <= NOW()

```

因此提醒不会永久丢失。

可以发送：

```
🔔 延迟提醒

交电费

原计划：15:00

```

***

# 29. notification\_targets 表

```
notification_targets
────────────────────────

id              BIGINT

name            VARCHAR(100)

channel         VARCHAR(50)
target          VARCHAR(255)
account_id      VARCHAR(255)
agent_id        VARCHAR(100)

enabled         TINYINT
is_default      TINYINT

created_at      DATETIME(3)
updated_at      DATETIME(3)

```

可以保存：

```
我的微信
Telegram
QQ

```

实际 target 和 accountId 作为配置数据保存，不写死进代码仓库。

***

# 30. OpenClaw Hook

RecallHub 到时间调用：

```
POST http://127.0.0.1:18789/hooks/agent

```

Header：

```
Authorization: Bearer <HOOK_TOKEN>

Content-Type: application/json

Idempotency-Key: recallhub:reminder:xxx

```

Payload：

```
{
  "message": "请只发送下面这条提醒，不添加解释：\n🔔 提醒\n\n续服务器",
  "name": "RecallHub Reminder",
  "agentId": "main",
  "deliver": true,
  "channel": "openclaw-weixin",
  "to": "<WECHAT_TARGET>",
  "accountId": "<WECHAT_ACCOUNT>",
  "waitForCompletion": true
}

```

OpenClaw 当前直接 `/hooks/agent` 支持 `channel`、`to`、`accountId` 和 `waitForCompletion`；`channel` 与 `to` 需要配套提供。

设置 `waitForCompletion=true` 后，HTTP 请求可以等待运行结束并返回执行和可用的投递结果。

***

# 31. Reminder 成功判定

不能只判断：

```
HTTP 200

```

因为 OpenClaw 文档明确说明 HTTP 成功并不自动证明模型执行成功或消息已经投递。

成功要求尽量满足：

```
completion.status = ok

deliveryAttempted = true

delivered = true

```

之后才：

```
Reminder.status = SENT

```

***

# 32. reminder\_deliveries 表

每次发送都留下记录：

```
reminder_deliveries
────────────────────────

id                  BIGINT

reminder_id         BIGINT

attempt_no          INT
idempotency_key     VARCHAR(255)

status              VARCHAR(20)

started_at          DATETIME(3)
finished_at         DATETIME(3)

http_status         INT
openclaw_run_id     VARCHAR(255)

delivery_attempted  TINYINT
delivered           TINYINT

error_code          VARCHAR(100)
error_message       VARCHAR(1000)

```

***

# 33. 重试策略

推荐：

```
第一次失败
↓
1 分钟

第二次失败
↓
5 分钟

第三次失败
↓
15 分钟

仍失败
↓
FAILED

```

以后可以增加：

```
微信失败
↓
Telegram 兜底

```

***

# 34. OpenClaw Hook Idempotency

每次发送：

```
recallhub:reminder:{reminderId}:delivery:{deliveryId}

```

作为：

```
Idempotency-Key

```

OpenClaw 当前 Hook 支持该 Header，并会用于请求重放控制。

RecallHub 自己仍需要维护 Delivery 状态。

不能把 OpenClaw 的幂等缓存当作永久 exactly-once 保证。

***

# 35. daily\_journals 表

```
daily_journals
────────────────────────

id                  BIGINT

journal_date        DATE

generated_content   LONGTEXT
content             LONGTEXT

user_edited         TINYINT

generated_at        DATETIME(3)

created_at          DATETIME(3)
updated_at          DATETIME(3)

```

***

# 36. 日记结构

一天可能存在：

```
DIARY 09:20
DIARY 14:30
DIARY 22:00

```

AI 可以根据这些内容生成：

```
当天整理后的日记

```

但是原始三个 Entry 永远保留。

如果以后模型更换，可以重新整理。

***

# 37. 搜索

V1 使用 MySQL FULLTEXT。

Entry：

```
FULLTEXT KEY ft_entry_content (title, content)
WITH PARSER ngram

```

MySQL 的 ngram parser 专门用于没有自然空格分词的 CJK 文本，并支持 InnoDB。

MySQL 官方提供的建索引方式也是：

```
FULLTEXT (title, body) WITH PARSER ngram

```

这一形式。

***

# 38. ngram 设置

默认先使用：

```
ngram_token_size = 2

```

MySQL ngram parser 默认 token size 就是 2。

如果以后发现单字搜索需求明显，再调整。

注意：

`ngram_token_size` 是启动配置。

改变以后需要重建相关 FULLTEXT 索引。

***

# 39. 搜索 API

```
GET /api/v1/search

```

参数：

```
q
type
status
from
to
page
size

```

例如：

```
/api/v1/search?q=Jev

```

或：

```
/api/v1/search?q=服务器&type=TODO

```

***

# 40. Capture API

这是 OpenClaw 最主要的写入接口。

```
POST /api/v1/captures

```

一次请求可以创建：

```
Source
Entry
Reminder

```

示例：

```
{
  "entry": {
    "type": "TODO",
    "title": "续服务器",
    "dueAt": "2026-09-30T23:59:59+08:00"
  },

  "reminder": {
    "remindAt": "2026-09-30T10:00:00+08:00"
  },

  "source": {
    "sourceType": "OPENCLAW",
    "channel": "WECHAT",
    "conversationId": "xxx",
    "messageId": "xxx"
  }
}

```

整个 Capture 使用一个数据库事务。

***

# 41. Batch Capture

语音聊天整理使用：

```
POST /api/v1/captures/batch

```

例如一段聊天提取出：

```
{
  "entries": [
    {
      "type": "DIARY",
      "content": "下午解决了登录接口的问题。"
    },
    {
      "type": "TODO",
      "title": "研究 Jev"
    },
    {
      "type": "IDEA",
      "content": "RecallHub 可以增加月度回顾。"
    }
  ]
}

```

***

# 42. Entry API

```
GET    /api/v1/entries/{id}

POST   /api/v1/entries

PATCH  /api/v1/entries/{id}

DELETE /api/v1/entries/{id}

```

删除采用软删除。

***

# 43. 完成待办

```
POST /api/v1/entries/{id}/complete

```

执行：

```
status = DONE

completed_at = NOW()

```

并取消该 Entry 尚未触发的 Reminder。

***

# 44. Reminder API

```
POST  /api/v1/reminders

PATCH /api/v1/reminders/{id}

POST  /api/v1/reminders/{id}/cancel

POST  /api/v1/reminders/{id}/snooze

```

延后：

```
{
  "until": "2026-09-30T11:00:00+08:00"
}

```

***

# 45. Today API

首页使用一个聚合接口：

```
GET /api/v1/today

```

返回：

```
{
  "date": "2026-09-20",
  "overdue": [],
  "reminders": [],
  "todos": [],
  "entries": [],
  "journal": null
}

```

前端不需要为了首页发十几个请求。

***

# 46. Timeline API

```
GET /api/v1/timeline

```

参数：

```
before
after
type
pageSize

```

使用游标分页优先于页码分页。

***

# 47. Journal API

```
GET /api/v1/journals/{date}

PUT /api/v1/journals/{date}

GET /api/v1/journals/{date}/entries

```

***

# 48. API 幂等

OpenClaw 写 Capture 时支持：

```
Idempotency-Key

```

例如：

```
openclaw:wechat:conversation123:message456

```

避免网络重试导致：

```
□ 续服务器
□ 续服务器

```

***

# 49. api\_idempotency 表

```
api_idempotency
────────────────────────

idempotency_key     VARCHAR(255)

request_path        VARCHAR(255)

resource_type       VARCHAR(50)
resource_id         BIGINT

created_at          DATETIME(3)
expires_at          DATETIME(3)

```

例如保留：

```
7 天

```

***

# 50. OpenClaw Skill

名称：

```
recall-hub

```

目录：

```
~/.openclaw/workspace/skills/recall-hub/

```

OpenClaw 当前将 workspace `skills/` 作为最高优先级的文件 Skill 来源之一，并通过 `SKILL.md` 定义技能。

目录：

```
recall-hub/
│
├── SKILL.md
│
└── scripts/
    └── recallhub.mjs

```

***

# 51. 为什么使用 mjs Helper

OpenClaw 本身依赖 Node 环境。

因此 Skill Helper 使用：

```
Node.js

```

避免额外依赖 Python。

Helper 负责：

```
HTTP
Authorization
JSON
Timeout
错误解析

```

Agent 不负责自己拼很长的 curl 命令。

***

# 52. Skill 行为

明确要求记录：

> 记一下

> 帮我保存

> 写进日记

必须调用 RecallHub。

***

表达未来行动：

> 周末研究 Jev。

创建：

```
TODO

```

***

明确要求提醒：

> 明天下午三点提醒我续服务器。

创建：

```
TODO
+
Reminder

```

***

记录当天发生事情：

> 今天下午把登录问题解决了。

根据上下文保存：

```
DIARY

```

***

普通聊天、知识问答默认不保存。

***

# 53. 时间不明确时

例如：

> 过几天提醒我。

应该询问具体时间。

不能随意决定。

但是：

> 明天下午三点

已经足够明确。

不再询问。

***

# 54. 主题系统定位

RecallHub V1 正式支持用户自定义主题。

目标是直接兼容类似下面的主题：

```
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.147 0.004 49.25);
  --primary: oklch(0.508 0.118 165.612);
  --radius: 0;
}

.dark {
  --background: oklch(0.147 0.004 49.25);
  --foreground: oklch(0.985 0.001 106.423);
  --primary: oklch(0.432 0.095 166.913);
}

```

也就是：

```
:root
↓
亮色

.dark
↓
暗色

```

***

# 55. 主题原则

页面组件禁止写：

```
background: #ffffff;
color: #111111;

```

应该写：

```
background: var(--card);
color: var(--card-foreground);

```

按钮：

```
background: var(--primary);
color: var(--primary-foreground);

```

Sidebar：

```
background: var(--sidebar);
color: var(--sidebar-foreground);
border-color: var(--sidebar-border);

```

***

# 56. 标准主题 Token

RecallHub V1 支持：

```
--background
--foreground

--card
--card-foreground

--popover
--popover-foreground

--primary
--primary-foreground

--secondary
--secondary-foreground

--muted
--muted-foreground

--accent
--accent-foreground

--destructive

--border
--input
--ring

--chart-1
--chart-2
--chart-3
--chart-4
--chart-5

--radius

--sidebar
--sidebar-foreground

--sidebar-primary
--sidebar-primary-foreground

--sidebar-accent
--sidebar-accent-foreground

--sidebar-border
--sidebar-ring

```

变量命名兼容目前常见的 shadcn 风格 globals.css，但 RecallHub 并不依赖 React 或 shadcn。

***

# 57. RecallHub 扩展 Token

个人信息系统还需要一些内容语义颜色。

额外支持：

```
--todo
--todo-foreground

--diary
--diary-foreground

--idea
--idea-foreground

--note
--note-foreground

--reminder
--reminder-foreground

--success
--success-foreground

--overdue
--overdue-foreground

```

例如：

```
.idea-badge {
  background: var(--idea);
  color: var(--idea-foreground);
}

```

***

# 58. 扩展 Token 缺失处理

导入的 globals.css 不要求包含 RecallHub 扩展 Token。

如果缺失：

```
--idea
--todo
--diary
...

```

使用 RecallHub 默认值。

因此普通 shadcn 风格主题也可以直接使用。

***

# 59. Theme 数据模型

新增：

```
themes
────────────────────────

id                  BIGINT

name                VARCHAR(100)
slug                VARCHAR(100)

light_variables     JSON
dark_variables      JSON

source_css          LONGTEXT

is_builtin          TINYINT

created_at          DATETIME(3)
updated_at          DATETIME(3)

```

MySQL 5.7 支持 JSON 类型，因此这里不需要退回 LONGTEXT 模拟 JSON。

***

# 60. Theme JSON

例如：

```
{
  "--background": "oklch(1 0 0)",
  "--foreground": "oklch(0.147 0.004 49.25)",
  "--primary": "oklch(0.508 0.118 165.612)",
  "--radius": "0"
}

```

亮色和暗色分别保存。

***

# 61. 外观模式

用户可以选择：

```
LIGHT
DARK
SYSTEM

```

默认：

```
SYSTEM

```

网页根据：

```
prefers-color-scheme

```

自动切换 `.dark`。

***

# 62. Theme Import

设置页面允许：

```
上传 globals.css

粘贴 globals.css

```

导入流程：

```
用户 CSS
   ↓
浏览器 CSS Parser
   ↓
检查规则
   ↓
提取 :root
   ↓
提取 .dark
   ↓
变量校验
   ↓
实时预览
   ↓
保存

```

***

# 63. 不直接执行任意 CSS

RecallHub 不允许用户导入的 CSS 任意影响页面。

允许：

```
:root {
  --primary: ...;
}

```

允许：

```
.dark {
  --background: ...;
}

```

不允许：

```
body {
  display: none;
}

```

不允许：

```
.sidebar {
  position: fixed;
}

```

不允许：

```
* {
  animation: ...;
}

```

***

# 64. Theme Parser

浏览器使用：

```
CSSStyleSheet

```

在不挂载到页面的情况下解析 CSS。

只允许：

```
CSSStyleRule

selector = :root

selector = .dark

```

出现：

```
@import
@font-face
@media
body
*
.className

```

则拒绝导入。

***

# 65. Theme Value 校验

颜色变量允许：

```
oklch(...)
hsl(...)
hsla(...)
rgb(...)
rgba(...)
#xxxxxx
transparent
currentColor

```

V1 禁止：

```
url(...)
image-set(...)
expression(...)

```

`--radius` 只接受：

```
0
px
rem
em

```

形式。

***

# 66. Theme Preview

导入主题后先进入预览。

预览内容至少包含：

```
背景

普通文字

Card

Primary Button

Secondary Button

Input

Sidebar

Todo

Diary

Idea

Reminder

Destructive

暗色模式

```

用户可以切换：

```
浅色
深色

```

确认以后才应用。

***

# 67. Theme Apply

前端最终生成：

```
<style id="recallhub-theme">
:root {
  ...
}

.dark {
  ...
}
</style>

```

主题切换不需要刷新页面。

***

# 68. Theme API

列表：

```
GET /api/v1/themes

```

详情：

```
GET /api/v1/themes/{id}

```

创建：

```
POST /api/v1/themes

```

修改：

```
PUT /api/v1/themes/{id}

```

删除：

```
DELETE /api/v1/themes/{id}

```

应用：

```
POST /api/v1/themes/{id}/activate

```

当前主题：

```
GET /api/v1/themes/current

```

导出：

```
GET /api/v1/themes/{id}/export

```

***

# 69. Theme Export

导出标准格式：

```
:root {
  --background: ...;
  --foreground: ...;
}

.dark {
  --background: ...;
  --foreground: ...;
}

```

因此：

```
网上找到主题
↓
导入 RecallHub

```

和：

```
RecallHub 调整主题
↓
导出 globals.css
↓
其他项目继续使用

```

都支持。

***

# 70. Theme 编辑器

设置 → 外观：

```
模式

[ 跟随系统 ] [ 浅色 ] [ 深色 ]


主题

RecallHub Default

My Theme

Evergreen


[ + 导入 globals.css ]

```

进入主题：

```
主题名称
[ My Theme ]

CSS
────────────────────────

:root {
   ...
}

.dark {
   ...
}

────────────────────────

实时预览

[恢复]
[保存]
[应用]

```

***

# 71. app\_settings 表

```
app_settings
────────────────────────

setting_key       VARCHAR(100)

setting_value     LONGTEXT

updated_at        DATETIME(3)

```

例如：

```
timezone
Asia/Shanghai

appearance
SYSTEM

theme_id
3

raw_chat_retention
ON_DEMAND

default_notification_target
1

```

***

# 72. 时区

数据库内部统一按 UTC 保存时间。

API 接收和返回：

```
ISO 8601

```

例如：

```
2026-09-20T15:00:00+08:00

```

系统设置保存：

```
Asia/Shanghai

```

业务逻辑不能依赖操作系统默认时区。

***

# 73. 用户认证

RecallHub V1 为单用户。

Web：

```
Spring Security
Session
HttpOnly Cookie
BCrypt

```

没有必要使用 JWT。

***

# 74. OpenClaw API Token

OpenClaw 不使用网页密码。

单独生成：

```
rh_xxxxxxxxxxxxxxxxxxxx

```

数据库保存：

```
token_hash
token_prefix

```

不保存明文 Token。

***

# 75. api\_tokens 表

```
api_tokens
────────────────────────

id              BIGINT

name            VARCHAR(100)

token_prefix    VARCHAR(20)
token_hash      VARCHAR(255)

scopes          JSON

enabled         TINYINT

created_at      DATETIME(3)
last_used_at    DATETIME(3)

```

***

# 76. Token 权限

可以包含：

```
ENTRY_READ
ENTRY_WRITE

REMINDER_READ
REMINDER_WRITE

SEARCH

JOURNAL_READ
JOURNAL_WRITE

```

OpenClaw Token 不拥有：

```
USER_ADMIN
TOKEN_ADMIN

```

***

# 77. 项目结构

```
recall-hub/
│
├── server/
│   │
│   ├── pom.xml
│   │
│   └── src/
│       ├── main/
│       │   ├── java/com/recallhub/
│       │   │
│       │   │   ├── RecallHubApplication.java
│       │   │   │
│       │   │   ├── common/
│       │   │   ├── auth/
│       │   │   ├── entry/
│       │   │   ├── source/
│       │   │   ├── reminder/
│       │   │   ├── journal/
│       │   │   ├── search/
│       │   │   ├── theme/
│       │   │   ├── setting/
│       │   │   ├── notification/
│       │   │   └── openclaw/
│       │   │
│       │   └── resources/
│       │       ├── application.yml
│       │       ├── application-local.yml
│       │       ├── application-prod.yml
│       │       └── db/migration/
│       │
│       └── test/
│
├── web/
│   │
│   ├── src/
│   │   ├── api/
│   │   ├── assets/
│   │   ├── components/
│   │   ├── layouts/
│   │   ├── pages/
│   │   ├── router/
│   │   ├── stores/
│   │   ├── styles/
│   │   │   ├── globals.css
│   │   │   └── theme.css
│   │   ├── theme/
│   │   └── types/
│   │
│   ├── package.json
│   └── vite.config.ts
│
├── openclaw/
│   └── recall-hub/
│       ├── SKILL.md
│       └── scripts/
│           └── recallhub.mjs
│
├── deploy/
│
├── docs/
│   ├── architecture.md
│   ├── api.md
│   ├── database.md
│   ├── themes.md
│   └── deployment.md
│
├── .env.example
└── README.md

```

***

# 78. Java 包结构

按照业务组织：

```
entry/
├── EntryController
├── EntryService
├── EntryMapper
├── EntryEntity
├── EntryType
└── dto/

```

而不是把全项目所有：

```
controller
service
mapper
entity

```

集中在四个大目录。

***

# 79. Flyway

数据库结构全部通过 Flyway。

例如：

```
V001__init.sql

V002__create_sources.sql

V003__create_entries.sql

V004__create_reminders.sql

V005__create_notification_targets.sql

V006__create_reminder_deliveries.sql

V007__create_journals.sql

V008__create_api_tokens.sql

V009__create_idempotency.sql

V010__create_themes.sql

V011__create_fulltext_indexes.sql

```

所有 migration 必须在：

```
MySQL 5.7.23

```

实际验证。

***

# 80. 前端主题约束

任何组件代码都不能写：

```
固定品牌蓝
固定背景白
固定黑色文字
固定圆角

```

所有这些必须来自 Theme Token。

例如：

```
.rh-card {
  background: var(--card);
  color: var(--card-foreground);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

```

***

# 81. 响应式设计

桌面：

```
左侧 Sidebar
+
主内容

```

手机：

```
Top Bar

Content

Bottom Navigation

```

移动端一级导航：

```
今天
时间线
+
日记
搜索

```

设置从头像菜单进入。

***

# 82. 外部访问

RecallHub 和 OpenClaw 都运行在家里电脑。

内部：

```
RecallHub
↓
127.0.0.1
↓
OpenClaw

```

Hook 不开放公网。

手机打开 RecallHub 建议：

```
Tailscale

```

或者：

```
HTTPS Reverse Proxy

```

不直接公开：

```
3306
18789

```

***

# 83. 环境变量

后端：

```
MYSQL_HOST

MYSQL_PORT

MYSQL_DATABASE

MYSQL_USERNAME

MYSQL_PASSWORD

RECALLHUB_TIMEZONE

OPENCLAW_BASE_URL

OPENCLAW_HOOK_TOKEN

BACKUP_PATH

```

例如：

```
OPENCLAW_BASE_URL=http://127.0.0.1:18789

```

***

# 84. Secret 原则

代码仓库绝不保存：

```
数据库密码

Hook Token

RecallHub API Token

微信 Target

微信 Account ID

```

这些内容通过：

```
环境变量
数据库配置

```

提供。

***

# 85. 日志

使用：

```
SLF4J
Logback

```

记录：

```
requestId
entryId
reminderId
deliveryId
OpenClaw runId
执行时间
错误类型

```

不能记录：

```
密码
Authorization
API Token
Hook Token
完整私人聊天

```

***

# 86. Health Check

启用：

```
Spring Boot Actuator

```

提供：

```
/actuator/health

```

另外：

```
GET /api/v1/system/openclaw/status

```

检测：

```
OpenClaw Gateway

```

***

# 87. 备份

每天自动：

```
mysqldump

```

目录：

```
backups/

```

保存策略：

```
最近 7 个每日备份

最近 4 个每周备份

最近 6 个每月备份

```

主题也在 MySQL 内，因此一起备份。

***

# 88. 数据导出

支持：

```
JSON
Markdown

```

Markdown：

```
export/
│
├── journals/
│   ├── 2026-09-20.md
│   └── ...
│
├── todos.md
├── ideas.md
└── notes.md

```

主题单独支持：

```
globals.css

```

导出。

***

# 89. 部署方式

由于本机已经存在：

```
MySQL 5.7.23

```

V1 不要求 Docker 运行数据库。

推荐：

```
Existing MySQL
+
RecallHub Spring Boot JAR
+
OpenClaw

```

RecallHub 后端可以通过：

```
systemd

```

常驻。

***

# 90. 前端生产部署

开发：

```
Vite Dev Server

```

生产：

```
npm run build

```

生成：

```
web/dist

```

可以最终一起打入 Spring Boot：

```
static/

```

因此正式环境只运行：

```
RecallHub Java 服务

```

即可同时提供 Web + API。

***

# 91. 自动测试

后端：

```
JUnit 5
Spring Boot Test
MockMvc

```

数据库集成测试必须使用：

```
MySQL 5.7

```

环境。

避免本地测试 8.x，生产却跑 5.7。

***

# 92. Reminder 测试

至少覆盖：

```
正常发送

OpenClaw 不在线

HTTP 500

HTTP Timeout

completion.status = error

delivered = false

重复发送

程序中途重启

过期 lease 恢复

关机后补发

```

***

# 93. Theme 测试

至少覆盖：

```
正常 :root

正常 .dark

OKLCH

HEX

RGB

radius = 0

radius = 0.5rem

缺少 dark

缺少部分变量

未知变量

body rule

@import

url()

损坏 CSS

实时切换 light / dark

```

***

# 94. 开发顺序

## 第一阶段

项目骨架：

```
Spring Boot

Vue

MySQL 5.7

Flyway

登录

基础 Layout

```

***

## 第二阶段

实现：

```
Entry

TODO

DIARY

NOTE

IDEA

```

以及：

```
今天

时间线

日记

```

***

## 第三阶段

实现：

```
Theme Token

Default Theme

Dark Mode

globals.css Import

Theme Preview

Theme Editor

Theme Export

```

主题系统必须在大量 UI 组件完成以前做好。

否则后面改主题成本会越来越高。

***

## 第四阶段

实现：

```
Search

MySQL ngram FULLTEXT

```

***

## 第五阶段

实现：

```
RecallHub REST API

API Token

Idempotency

```

***

## 第六阶段

安装：

```
RecallHub OpenClaw Skill

```

完成：

```
微信 / TG / QQ
↓
OpenClaw
↓
RecallHub

```

***

## 第七阶段

实现：

```
Reminder Worker

OpenClaw Hook

微信提醒

重试

Delivery 日志

```

***

## 第八阶段

实现：

```
Batch Capture

聊天整理

日记整理

```

***

## 第九阶段

实现：

```
自动备份

JSON 导出

Markdown 导出

Theme 导出

```

V1 完成。

***

# 95. V1 验收标准

能够从 Web 创建：

```
TODO
DIARY
NOTE
IDEA

```

***

能够从微信告诉 OpenClaw：

> 记一下 RecallHub 以后增加月度回顾。

然后网页出现对应 IDEA。

***

能够告诉 OpenClaw：

> 明天下午三点提醒我交电费。

数据库出现：

```
TODO
+
Reminder

```

***

Reminder 到期以后：

```
微信收到消息

```

***

RecallHub 重启以后：

```
未来 Reminder 不丢失

```

***

OpenClaw 离线时：

```
Reminder 不误标 SENT

```

恢复以后：

```
能够重新发送

```

***

搜索：

```
Jev

```

能够找到相关：

```
日记
待办
想法
备忘

```

***

可以导入：

```
:root {
  --primary: oklch(...);
}

.dark {
  --primary: oklch(...);
}

```

并立即看到主题预览。

***

应用主题以后：

```
今天
时间线
日记
搜索
设置

```

全部使用新主题。

***

刷新网页以后主题仍然存在。

***

切换：

```
浅色
深色
跟随系统

```

都正常。

***

能够导出当前主题为：

```
globals.css

```

***

能够导出所有数据：

```
JSON
Markdown

```

***

# 96. V1 最终技术基线

```
项目
RecallHub

JDK
17

Backend
Spring Boot 3.5.x

Build
Maven

Database
MySQL 5.7.23

Storage Engine
InnoDB

Charset
utf8mb4

Collation
utf8mb4_unicode_ci

Persistence
MyBatis + MyBatis-Plus

Migration
Flyway

Frontend
Vue 3

Language
TypeScript

Bundler
Vite

Styling
Tailwind CSS
+
CSS Custom Properties

Theme Format
:root {}
.dark {}

Theme Color
OKLCH / RGB / HSL / HEX

Authentication
Spring Security Session

Agent
OpenClaw

Agent Integration
Skill + REST API

Reminder Scheduler
Spring Scheduling + MySQL

Reminder Delivery
OpenClaw /hooks/agent

Primary Reminder Channel
微信

Search
MySQL FULLTEXT + ngram

Remote Access
Tailscale / HTTPS

Backup
mysqldump

Export
JSON + Markdown + globals.css

```

***

# 97. 最终数据流

记录：

```
用户
↓
微信
↓
OpenClaw
↓
RecallHub Skill
↓
Capture API
↓
MySQL

```

查看：

```
浏览器
↓
RecallHub
↓
API
↓
MySQL

```

提醒：

```
MySQL
↓
Reminder Worker
↓
OpenClaw Hook
↓
微信
↓
用户

```

日记：

```
聊天 / 网页
↓
DIARY Entry
↓
每日多个原始片段
↓
整理
↓
Daily Journal

```

主题：

```
globals.css
↓
Theme Parser
↓
Token Validation
↓
Preview
↓
MySQL
↓
CSS Variables
↓
整个 RecallHub UI

```

***

# 98. 后续版本

V1 稳定使用以后再考虑：

```
每周回顾

月度回顾

长期未完成任务提醒

Telegram 自动兜底

语义搜索

Embedding

Qdrant

Jev 自动分类

图片附件

文件附件

语音文件

OpenClaw Plugin

自动会话结束处理

重复提醒

任务优先级

标签

关联记录

```

这些功能不能影响 V1 的基本架构。

RecallHub 的核心长期保持：

```
OpenClaw 负责交流

RecallHub 负责数据

MySQL 负责状态

主题负责外观

```
